<div class="container">
    {% for name, messages in flash.getMessages() %}
        <div class="alerte alerte-{{ name }}">
            <button type="button" class="close-alert">
                <span>&times;</span>
            </button>
            {% for message in messages %}
                {{ message }}
            {% endfor %}
        </div>
    {% endfor %}
</div>
