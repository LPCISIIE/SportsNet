<nav class="navbar navbar-default navbar-static-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}">SportNet</a>
        </div>
        <div class="collapse navbar-collapse" id="navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if auth.check() %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ auth.user.email }} <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path_for('user.compte') }}">Mon compte</a></li>
                            <li><a href="{{ path_for('user.epreuves') }}">Mes épreuves</a></li>
                            {% if auth.user.checkOrganisateur() %}
                                <li><a href="{{ path_for('user.events') }}">Mes événements</a></li>
                                <li><a href="{{ path_for('evenement.create') }}">Créer un événement</a></li>
                            {% endif %}
                            <li><a href="{{ path_for('logout') }}">Logout</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li><a href="{{ path_for('register') }}">Register</a></li>
                    <li><a href="{{ path_for('login') }}">Login</a></li>
                {% endif %}
            </ul>
            <form action="{{ path_for('search') }}" class="navbar-form navbar-right">
                <input type="text" name="q" class="form-control" placeholder="Recherche">
            </form>
        </div>
    </div>
</nav>
